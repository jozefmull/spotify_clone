import React from 'react'

import styles from '../../css/MusicPlayer.module.css'

const VolumeBar = () => {
  return (
    <div className={styles.volume_bar}>VolumeBar</div>
  )
}

export default VolumeBar